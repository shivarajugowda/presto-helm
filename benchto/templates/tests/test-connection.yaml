apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "benchto.fullname" . }}-test-connection"
  labels:
    {{- include "benchto.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "benchto.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
